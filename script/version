#!/usr/bin/env bash

function version {
  local shome="$(cd -P -- "${BASH_SOURCE%/*}/.." && pwd -P)"
  source "$shome/script/profile"

  cd "$shome"
  local ver_py36="$(python --version 2>&1 | awk '$1 == "Python" {print $2}' || true)"
  jq -n --arg py36 "$ver_py36" '{py36: $py36}'
}

version "$@"
